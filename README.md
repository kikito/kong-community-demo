# Kong 2.1 Community Demos

06-28-2020

## Instructions:

* Enter each folder
* Run `docker-compose up` to set up the demo env
* Run the steps in `demo.sh`
* Run `docker-compose down` to finish the demo env / cleanup

